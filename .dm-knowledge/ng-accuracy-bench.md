- **[convention]** Golden corpora scope: definitions exclude imports, namespace/module declarations and enum members; edges are call/type-use/function-valued references in a symbol BODY (never its signature), deduped per (from symbol, to symbol) - not per (from, to) string pair, so an overload set or a field/accessor pair sharing a `file:qualified` key stays two distinct edges, disambiguated by `from_line`/`to_line`. Both endpoints must be definitions in the same golden. docs/accuracy.md carries the baseline and the reasoning  _(2026-08-27T20:44:52Z)_
- **[pitfall]** Never lower a floor in tests/golden/floors.json to make a build pass - a measurement that dropped is the finding, unless the golden it's measured against changed underneath it, in which case `--update-floors --lower-floors --reason "<why>"` accepts the drop deliberately (all-or-nothing per run; the reason prints but isn't persisted in floors.json, so put it in the commit and docs/accuracy.md's fix-round table)  _(2026-08-27T20:44:52Z)_
- **[command]** `zig build bench` scores the indexer against the hand-verified golden corpora in tests/golden/ and fails when a language drops below any of its seven floors (def/edge precision+recall, exact agreement, site precision+recall) in tests/golden/floors.json; `zig build bench -- --update-floors` re-records them (ratchet-only), `-- --propose <fixture-root>` drafts a golden skeleton (with from_line/to_line) to audit by hand  _(2026-08-27T20:45:01Z)_
