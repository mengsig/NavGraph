- **[invariant]** Resolver contract: abstain on an UNKNOWN receiver, resolve when the receiver's type IS known (typed param incl. C-family 'const T* x', field of the enclosing type, Lua colon receiver, import/module binding); a call NEVER binds to a non-callable (isCallable + Language.callMayTargetType); any receiver-guessed edge must stay exact=false so --strict drops it.  _(2026-08-27T21:12:24Z)_
- **[pitfall]** Import evidence (hasNonLocalImportBinding) may only suppress the global-name fallback for languages whose imports the indexer actually resolves (Language.resolvesImportBindings, mirroring the capabilities.zig manifest split). Rust 'use' is unmodelled, so treating a non-resolved binding as proof deleted every Rust cross-file call edge.  _(2026-08-27T21:12:37Z)_
- **[invariant]** Perf gate: whole-project link passes must stay linear in project size. Java supertypes are precomputed once per build (Index.java_bases); do not reintroduce a per-reference symbol-table scan. Production bound: 32 copies of testenv/ (4,192 files) warm well under 250 ms (main baseline 120 ms; was 4,428 ms). Guarded by the Zig test 'Java inherited-member resolution stays linear in symbol count' - a same-run ratio, NOT a wall clock: a 6-group corpus and a 120-group corpus (20x scale), each timed as the fastest of 3 builds, bounded at 8x the linear expectation. Measured on 16 cores: linear 12.9-44.6, the pre-fix quadratic tree 257-295, so the bound has >=3.6x headroom and still fails the quadratic build. Re-measure both populations before touching 8x or 20x - at 10x scale the quadratic penalty was only ~6x linear and no safe bound existed.  _(2026-08-28T00:30:28Z)_
- **[convention]** CLI flags split two ways: global-class (-v -d -C -l -t -s -r -j --no-cache) are accepted on EVERY command and, where unused, ignored with a stderr note at exit 0 (registry.isGlobalClassOption); command-specific flags on the wrong command stay exit 2. A format the command cannot emit is still an error. capabilities -j is the authoritative per-command matrix; README must match it.  _(2026-08-27T21:12:37Z)_
- **[pitfall]** Never write to stderr from inside a Zig test block: the build runner prints 'failed command:' on a fully green run and a human reads it as failure. index.zig warnParseHealth is gated on builtin.is_test for this reason.  _(2026-08-27T21:12:37Z)_
- **[convention]** build.zig.zon .paths must list build.zig, build.zig.zon, src, tests, testenv, README.md and prompt.md — the test step drives the tests/ contract suites, which read testenv/ fixtures and assert on README.md/prompt.md. Verify by running 'zig build test' in a tree pruned to exactly .paths.  _(2026-08-27T21:12:43Z)_
- **[invariant]** Resolver contract (round 2, refines the line above): a binding for the qualifier ALWAYS answers, untyped included - a local shadows a same-named field. The enclosing type's field table answers only when the receiver chain's HEAD has a known type (self/this, or a typed local; Go method receivers and 'name T' params are now typed), which model.Reference.receiver_root records. A Go package qualifier must head its chain (goPackageTarget), which is what keeps 'a.store.Stats()' off 'struct Stats' now that Go calls may target a type again (conversions). A call may bind to a VALUE (const/var may hold a function); only types are refused.  _(2026-08-27T22:46:44Z)_
- **[invariant]** The -l flag is explicit, not a sentinel: query.Options.limit_set says the caller asked for a cap, so an explicit 300 is a cap of 300 on imports/importers/graph/hot. Any code that sets options.limit programmatically must set limit_set too (cli, agent_api, the hard-budget compaction). 'graph -l N' must report what it withheld: nodes_total+truncated in -j, a stderr note for the HTML page.  _(2026-08-27T22:46:51Z)_
- **[pitfall]** Keep a diagnostic's message testable: split the formatter (writeParseHealthWarning, takes a *Writer) from the stderr sink (warnParseHealth, still gated on builtin.is_test). Gating the whole function on is_test made its assertions and format string unreachable from the suite.  _(2026-08-27T22:46:51Z)_
- **[convention]** Perf gates assert a SAME-RUN ratio, never wall-clock: build a small corpus in the same test and bound the full build at a multiple of the linear expectation. A saturated runner slows both, so the assertion does not drift with machine load. Two rules the first version got wrong: take the FASTEST of a few runs per size (jitter is one-sided), and pick the multiplier from measured linear and regressed populations - never assume the regression is far away.  _(2026-08-28T00:30:28Z)_
- **[invariant]** Resolver contract (round 3, supersedes the callMayTargetType half of the two lines above): Go is NOT a callMayTargetType language. goPackageTarget reclassifies a package-qualified call that lands on a type to RefKind.type_use ('models.WidgetID(n)' is a conversion), so the edge survives while no call edge in the graph binds to a non-callable - asserted project-wide in two index tests. goPackageTarget also abstains on isLocalBinding(qualifier): a local named like an imported package shadows it. Go short declarations bind every name on the left, at a statement keyword as well as at line start ('for _, x := range', 'x, err :=', 'if x, ok :='), and Go grouped params share the trailing type ('store, other *Local'). A bare qualifier resolving to a .module symbol (package/namespace/mod clause) is never exact - inferred when the pick was confident, ambiguous when not.  _(2026-08-28T00:30:28Z)_
