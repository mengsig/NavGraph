- **[invariant]** navgraph lsp owns stdout: it is the JSON-RPC protocol channel. All logging goes to stderr or --log <file>. A stray print to stdout corrupts every client session; CI asserts an lsp session emits only frames  _(2026-08-27T19:57:04Z)_
- **[invariant]** src/lsp is single-threaded by construction: one timed read on stdin drives requests, the debounce flush and the mtime watcher. The index, overlays, reader and writer are owned by that one thread, so the graph needs no lock. Do not add a watcher thread  _(2026-08-27T19:57:04Z)_
- **[invariant]** index.assemble copies each symbol's refs into the Index arena because resolution writes ref.target in place; the ParsedFile parse output stays pristine so it can be re-assembled any number of times. Never share refs between two live Indexes  _(2026-08-27T19:57:04Z)_
- **[pitfall]** query.callSiteLines grows its output ArrayList with idx.gpa, so a caller must deinit that list with idx.gpa too - not with its own request arena, which leaks under the testing allocator  _(2026-08-27T19:57:04Z)_
- **[pitfall]** session.Session frees a re-parsed file's old arena only after the replacement index is in place (the retired list): the live index still points into it. Freeing earlier dangles every served response  _(2026-08-27T19:57:04Z)_
- **[convention]** the parse cache is never written while a document is open - the live index then holds unsaved buffer text and the cache is keyed by disk mtime+size (cache.write asserts stat.size == file.text.len)  _(2026-08-27T19:57:04Z)_
- **[routing]** adding a navgraph/* method is one adapter function in src/lsp/queries.zig plus one entry in the requests table in src/lsp/handlers.zig; the table also generates experimental.navgraph.methods, so only implemented methods are advertised  _(2026-08-27T19:57:04Z)_
- **[command]** zig build test --summary all runs the whole suite (mod + exe test binaries); zig build -Doptimize=ReleaseFast is what the editor server ships as and what docs/lsp.md's numbers were measured in; CI runs both on ubuntu-latest and macos-latest with zig 0.16.0  _(2026-08-27T19:57:11Z)_
- **[convention]** the editor-server verb is 'navgraph lsp'. 'serve' is the MCP/agent-API verb (src/agent_api.zig); keep the two surfaces and their modules (src/lsp vs src/agent_api) distinct  _(2026-08-27T19:59:37Z)_
