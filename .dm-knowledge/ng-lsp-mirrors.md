- **[pitfall]** src/cli.zig hand-aligns its Command enum/map literals in a denser style than `zig fmt` would produce. CI's `zig fmt --check .` gate (added by the parent branch) passes on it as-is; don't run `zig fmt` on that file — it rewrites unrelated lines and the file must stay fmt-clean by hand.  _(2026-08-28T02:59:51Z)_
- **[convention]** The release workflow (.github/workflows/release.yml) cross-compiles all 4 targets (x86_64/aarch64 linux/macos) from a single ubuntu-latest runner via zig -Dtarget=...; NavGraph is pure Zig/std with no platform SDK dependency, so no macOS runner is needed.  _(2026-08-27T20:31:05Z)_
- **[convention]** The `lsp` verb sits in `src/command_registry.zig` between `serve` and `help` in both the `Command` enum and `command_descriptors` — the ordinal invariant `capabilities -j` checks. Its descriptor: `server_available = false` (it never runs headless), options `{root, no_cache, log, log_level}` appended at the matching `option_descriptors` slots, `access = .server`, `requires_index = true`, `outputs = rpc_only`.  _(2026-08-28T02:59:51Z)_
- **[invariant]** `Session.cacheWrite` (src/lsp/session.zig) gates a cache refresh on three conditions: `cache.enabled`, `cache_stale`, and `overlays.count() == 0` (no open document — its unsaved text must never reach a cache a CLI run later trusts). The write itself happens inside `index.assemble`, after `resolveReferences` but before `applyRouterMounts`: the mount pass rewrites route symbol names in place, and a prefixed name reaching the per-file cache would double-prefix on the next build.  _(2026-08-28T02:59:51Z)_
