- **[command]** tree-sitter backend: build with -Dtree-sitter=<all|none|comma list of python,typescript,tsx> (default all); run it with --backend tree-sitter (--backend auto, the default, still uses the heuristic scanner for every language)  _(2026-08-27T21:08:41Z)_
- **[command]** zig build differ runs tests/backend-differ.zig: both parser backends over testenv/{py_fastapi,ts_frontend,fullstack,parser_gaps}, asserting no definition/edge lost and no unreviewed new exact edge; it is also part of zig build test  _(2026-08-27T21:08:41Z)_
- **[pitfall]** the tree-sitter runtime comes from the maintainers' npm tarball, not the GitHub source archive: b.dependency executes a dependency's build.zig and upstream's uses Compile.addCSourceFile, removed in zig 0.16  _(2026-08-27T21:08:41Z)_
- **[pitfall]** ts_query_new walks the whole grammar (~14ms); compiling a query per file is a 37x slowdown. ts_query_capture_name_for_id returns TSQuery-owned memory - never store the slice. Both are structurally prevented in src/ts_backend.zig (Compiled holder, capture ids mapped to enums once)  _(2026-08-27T21:08:41Z)_
- **[pitfall]** grammar C sources need -std=gnu11: strict ISO hides le16toh/be16toh/fdopen that lib/src/unicode.h and parser.c use unguarded. tsx is a separate generated grammar from typescript, not a mode of it  _(2026-08-27T21:08:41Z)_
- **[invariant]** @embedFile cannot escape the Zig module root, so the query sets live at src/queries/<lang>/*.scm (not repo-root queries/); build.zig's srcFingerprint hashes src/**.scm as well as src/**.zig so a query edit invalidates the on-disk cache  _(2026-08-27T21:08:41Z)_
- **[invariant]** cache entries are keyed on the backend that produced them: index.zig refuses a restored entry unless backends.cacheEntryUsable(lang, choice, health) accepts it, and build.zig folds the linked grammar set into cache_key. Without both, --backend selects nothing whenever a cache exists and a -Dtree-sitter=none binary serves an all binary's cache  _(2026-08-28T03:14:40Z)_
- **[invariant]** exportedness is per-language in BOTH backends: Python uses the leading underscore, TypeScript uses the export keyword and an export does not reach through a class/interface/enum/type-alias/object-literal body (isMemberList in ts_backend.zig is the one list all five share - a node type missing from it silently inherits export, which is what happened to interface_body until round 2). tests/backend-differ.zig compares the flag for every shared symbol - Key deliberately excludes it, so nothing else notices a drift  _(2026-08-28T03:14:50Z)_
- **[invariant]** an object literal's methods (`export const h = { save() {} }`) are parented to the binding that owns the literal, not left parentless: Def.object_member is set when a method_definition's direct parent is an `object` node, and resolveParents parents it to `nearest` (the enclosing binding) rather than requiring a container kind. Without this, collisions sees a false duplicate between the method and any top-level function of the same name  _(2026-08-28T08:00:00Z)_
- **[invariant]** Reference.receiver_root on the tree-sitter side is derived from the tree (ts_backend.chainRoot), not from a query capture: a capture needs one pattern per chain depth and drops the head. The resolver reads it to tell a field access from a bare module qualifier (PR #7)  _(2026-08-28T03:14:50Z)_
- **[pitfall]** ts_backend keeps defs sorted outer-before-inner by span_start and spans nest, so the innermost callable containing an offset is on the nearest-chain of the last def opened before it. Do not reintroduce a per-site linear scan over defs: it made one 12k-definition module quadratic (1.141s vs 0.268s)  _(2026-08-28T03:14:50Z)_
- **[pitfall]** a tree-sitter query cursor's match limit persists across ts_query_cursor_exec but the capture-list pool only refuses a list it has not already grown to hold - forcing an overflow in a test needs a FRESH Registry with the limit set before the first parse  _(2026-08-28T03:14:50Z)_
- **[invariant]** land this branch before PR #8 (feat/x/lsp-server): PR #8 is mostly new files under src/lsp/ (~9500 of +10085 lines), while this branch changes index.build's signature and rewrites all forty command_descriptor rows - far more expensive to re-derive on top of PR #8 than the reverse. No NGCACHE collision either way: PR #8's src/cache.zig still reads NGCACHE11 (unchanged from main) and does not touch cache.zig at all  _(2026-08-28T08:30:00Z)_
- **[pitfall]** merging PR #8 on top of this branch: src/index.zig is the real conflict - PR #8 splits Builder.build into collect(...)+assemble(gpa, Assembly) and adds parseOne/ParseOutput; this branch adds required Builder fields backend: backends.Choice and registry: *backends.Registry and rewrites parseFile to call backends.parse. collect must thread Choice through or it silently hardcodes .auto  _(2026-08-28T08:30:00Z)_
- **[invariant]** post-merge design decision, not automatic: backends.Registry currently lives on buildOpenDir's stack frame, recompiled every build (~14ms/language). An LSP session re-indexes on every didChange, so Registry must move to session lifetime so queries compile once per process - decide this deliberately, a clean merge that leaves it per-build is a silent perf regression no test catches  _(2026-08-28T08:30:00Z)_
- **[pitfall]** merging PR #8: src/command_registry.zig gets a guaranteed textual conflict (this branch touches all forty rows; PR #8 adds an lsp row + Option members) - the new lsp row needs .backend added by hand. src/capabilities.zig: both branches change the manifest, so recompute the schemaHash contract expectation once, after the merge, not once per branch  _(2026-08-28T08:30:00Z)_
