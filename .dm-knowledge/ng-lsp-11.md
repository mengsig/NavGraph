- **[pitfall]** hierarchy.zig's appendSegment/scanRustImpls used to hardcode idx.gpa for edges.append while build()'s caller-supplied gpa freed the result (Graph.deinit) - fine only because every prior caller passed idx.gpa itself. Passing a request arena (LSP adapters do) leaked/mismatched allocators. Fixed by threading gpa through the whole appendDeclaredBases family; keep any new hierarchy.zig call site passing its own gpa consistently.  _(2026-08-28T09:35:22Z)_
- **[pitfall]** LSP JSON writers that hand-roll brace/bracket closing (queries.writeCodeLens et al.) are easy to under-close silently: a missing brace still produces syntactically-plausible-looking JSON when eyeballed, only failing at std.json.parseFromSlice. Write a dedicated test per method that actually parses the response before trusting a hand-rolled writer.  _(2026-08-28T10:25:23Z)_
- **[convention]** 1.1 content hashing (Symbol.contentHash, navgraph/impact's changeId) uses std.hash.Wyhash with an 8-byte ASCII seed tag (e.g. 0x4e_47_43_4f_4e_54_45_4e = "NGCONTEN"), formatted as 16 lowercase hex chars — matches capabilities.zig's existing Wyhash-fingerprint convention. contentHash is whitespace-normalised (collapsed to single spaces) over Symbol.body(); changeId hashes only hunk shape (file+line range), not content bytes.  _(2026-08-28T10:25:28Z)_
- **[convention]** The `navgraph hunks`/`context`/`where` CLI verbs and `navgraph.hunks`/`.context`/`.where` MCP tools (src/lsp/mirrors.zig) each build a throwaway one-shot `lsp.session.Session` per call and call `queries.writeImpact`/`writeContext`/`writeWhere` directly, instead of threading a `Session` through main.zig's `*Index`-based CLI dispatch or extending agent_api's typed envelope. `hunks` is named that, not `impact`, because `impact` was already an alias of the pre-1.1 `affected` command (a different, older feature) — reusing it would have silently repurposed an existing CLI contract. `context`'s `--budget` is a *token* count (0 means "use the 2000-token default", never a usage error), a different unit from the CLI's shared `--budget` hard-byte-output ceiling of the same name; cli.zig's `parseFlag` special-cases `out.command == .context` to keep the shared option's 64-byte floor from applying to it. Known tradeoff, accepted rather than solved: the three MCP tools re-walk the project every call, unlike `navgraph.query`, which reuses `ServerSession`'s resident index — `Session` always builds its own index rather than adopting an existing one, so sharing that index here would mean carrying two synchronized indices for three tools whose call volume doesn't yet justify it.  _(2026-08-28T13:57:57Z)_
