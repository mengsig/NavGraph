- **[invariant]** Resolver contract (wave 1): resolution order for a qualified ref is self -> receiver type (local/param binding, then the enclosing type's field table when the chain head is typed) -> constructor-keyword type -> same-file container -> imported container -> imported module -> Go package -> proven non-local import -> same-file owner that declares the member -> enclosing type's FIELD named by a bare qualifier -> builtin-type qualifier abstain -> heuristic method guess. Only the last is non-exact by construction; every earlier branch that binds is exact when its pick is unambiguous.  _(2026-08-28T02:04:35Z)_
- **[invariant]** A receiver whose DECLARED type is a standard-library container (Language.isBuiltinContainer) never binds to a project method - the abstain happens instead of the global name guess. Rust smart pointers are the exception on the receiver side only: Language.derefsToInner says Box/Rc/Arc/RefCell/Cow (and, in the C family, unique_ptr/shared_ptr/weak_ptr) deref to the inner type, so a receiver of that type must NOT abstain, while 'Box::new' as a qualifier still must. Conditional on cDeclarator (parser.zig) actually capturing a type: it only walks a `::`-scoped chain, not `.`, so a package-qualified declared type (`java.util.List<Repo> pending`) captures no type at all and falls through to the unconstrained heuristic guess instead of abstaining - reachable today only on an unimported/fully-qualified name, since an imported `List<Repo> pending` resolves its type through the import table first.  _(2026-08-28T02:04:35Z)_
- **[pitfall]** Java/C#/C/C++ write the type before the name (Language.declaresTypeBeforeName). Java and C# used to use the annotation-language 'name: Type' param splitter, which bound the TYPE as the name and shadowed same-named globals. The C declarator must also count generic depth itself (skipGenericArgs): '<' is not in the bracket table, so 'Repository<Product> products' parsed as nothing.  _(2026-08-28T02:04:35Z)_
- **[convention]** Container symbols carry their FIELD types as model.Binding via parser collectMemberBindings (C-family, Zig) or collectBindings over a record body (Rust). Bracketed runs are skipped so a method local never enters the type's field table - a local leaking in there mis-types another method's receiver.  _(2026-08-28T02:04:35Z)_
- **[invariant]** Ruby specifics the resolver depends on: attr_accessor/reader/writer and operator methods (def +, def [], def <=>) are DEFINITIONS; every 'x.y' with a receiver is a call (no public fields, no parens needed); a predicate/bang sigil belongs to the name at the call site as well as the def; Index.type_bases carries a class's superclass and mixins AND, for a module, the classes that include it (a mixin's methods dispatch into the includer); 'Klass.new' resolves to the class's 'initialize'.  _(2026-08-28T02:04:35Z)_
- **[convention]** A '::'-scoped type that only scopes the function after it (Rust 'Cents::from_value', Ruby 'Tricky::Ledger.created') is NOT a type use - the edge is to the function. '::new' / '.new' is, since it constructs, and an uppercase member ('Val::Number') is an enum variant of the type. isScopeQualifierOnly in parser.zig.  _(2026-08-28T02:04:35Z)_
- **[pitfall]** Known minor gaps left open by wave 1 (coldstart review F9, none a phantom, none fixed this round - recall-neutral or cosmetic): `followFunctionAlias` (index.zig) rewrites `ref.target` but leaves `resolution_reason` describing how the ALIAS was found, so an edge can report a stale reason for a target reached by a different route; `cDeclarator` (parser.zig) rejects `Widget* const wc_;` (const-after-star) as a modifier, producing no binding where `const Widget* cw_;` binds exact - the edge still exists via the heuristic; `addRubyMixinIncluders` (index.zig) does not dedupe an included module listed twice, so a class that `include`s the same module twice appears twice in its base list (harmless, no double edge); the Java/C# keywords added to `c_decl_modifiers` (parser.zig) are shared with C/C++, so a C variable literally named `internal`/`partial`/`final`/`readonly`/`sealed`/`override`/`virtual`/`transient` now yields no binding (forgoes a binding, no phantom).  _(2026-08-28T07:30:00Z)_
