- **[invariant]** Resolver contract (wave 1): resolution order for a qualified ref is self -> receiver type (local/param binding, then the enclosing type's field table when the chain head is typed) -> constructor-keyword type -> same-file container -> imported container -> imported module -> Go package -> proven non-local import -> same-file owner that declares the member -> enclosing type's FIELD named by a bare qualifier -> builtin-type qualifier abstain -> heuristic method guess. Only the last is non-exact by construction; every earlier branch that binds is exact when its pick is unambiguous. followFunctionAlias runs AFTER all of them and may retarget an edge through an alias constant; it never raises exactness, because the hop that found the alias may itself have been a guess (two files each exporting 'const doubler = ...').  _(2026-08-28T02:04:35Z)_
- **[invariant]** A receiver whose DECLARED type is a standard-library container (Language.isBuiltinContainer) never binds to a project method - the abstain happens instead of the global name guess. Rust smart pointers are the exception on the receiver side only: Language.derefsToInner says Box/Rc/Arc/RefCell/Cell/Cow/Mutex/RwLock (and, in the C family, unique_ptr/shared_ptr/weak_ptr/optional) deref to the inner type, so a receiver of that type must NOT abstain, while 'Box::new' as a qualifier still must. The abstain is 'isBuiltinContainer and !derefsToInner', so it never fires for a deref type: a call to the WRAPPER's own member (u_.reset(), w_.lock()) still reaches a same-named project method by the global-name guess - a known gap, pinned by a test. Conditional on cDeclarator (parser.zig) actually capturing a type: it only walks a `::`-scoped chain, not `.`, so a package-qualified declared type (`java.util.List<Repo> pending`) captures no type at all and falls through to the unconstrained heuristic guess instead of abstaining - reachable today only on an unimported/fully-qualified name, since an imported `List<Repo> pending` resolves its type through the import table first.  _(2026-08-28T02:04:35Z)_
- **[pitfall]** Java/C#/C/C++ write the type before the name (Language.declaresTypeBeforeName). Java and C# used to use the annotation-language 'name: Type' param splitter, which bound the TYPE as the name and shadowed same-named globals. The C declarator must also count generic depth itself (skipGenericArgs): '<' is not in the bracket table, so 'Repository<Product> products' parsed as nothing.  _(2026-08-28T02:04:35Z)_
- **[convention]** Container symbols carry their FIELD types as model.Binding via parser collectMemberBindings (C-family, Zig) or collectBindings over a record body (Rust). Bracketed runs are skipped so a method local never enters the type's field table - a local leaking in there mis-types another method's receiver.  _(2026-08-28T02:04:35Z)_
- **[invariant]** Ruby specifics the resolver depends on: attr_accessor/reader/writer and operator methods (def +, def [], def <=>) are DEFINITIONS; every 'x.y' with a receiver is a call (no public fields, no parens needed); a predicate/bang sigil belongs to the name at the call site as well as the def; Index.type_bases carries ANCESTORS ONLY (a class's superclass and the modules it mixes in), in Ruby MRO order - last 'include' first, then earlier ones, then the superclass. The reverse mixin edge (module -> the classes that include it) lives in its own Index.module_includers and is read ONLY for implicit self inside a module body; a base-chain walk must never cross it, or two sibling classes that include the same module resolve each other's 'super'. A bare 'super' is a RefKind.call, not a read - as a read it was invisible to the call graph and to the accuracy bench, so no fixture could regress on it. 'Klass.new' resolves to the class's 'initialize'.  _(2026-08-28T02:04:35Z)_
- **[convention]** A '::'-scoped type that only scopes the function after it (Rust 'Cents::from_value', Ruby 'Tricky::Ledger.created') is NOT a type use - the edge is to the function. '::new' / '.new' is, since it constructs, and an uppercase member ('Val::Number') is an enum variant of the type. isScopeQualifierOnly in parser.zig.  _(2026-08-28T02:04:35Z)_
- **[pitfall]** Known minor gaps left open by wave 1 after fix round 2 (none a phantom; each recall-neutral or cosmetic, and each now visible as a bench finding rather than graded away): `cDeclarator` (parser.zig) rejects `Widget* const wc_;` (const-after-star) as a modifier, producing no binding where `const Widget* cw_;` binds exact - the edge still exists via the heuristic; the Java/C# keywords added to `c_decl_modifiers` (parser.zig) are shared with C/C++, so a C variable literally named `internal`/`partial`/`final`/`readonly`/`sealed`/`override`/`virtual`/`transient` now yields no binding; `isDirectInit` (parser.zig) only accepts a literal or braced first argument, so `Car car(std::make_unique<Engine>(), ...)` reads as the most vexing parse and `car.goUnique()` stays heuristic though C++ proves it; a smart-pointer deref (`engine_->start()`) binds but never exact; a Ruby `super` is exact only when the class declares exactly one base, so a class with a superclass AND a mixin stays inferred. The last three are graded exact=true in the goldens on purpose - they are the gaps, not the grading.  _(2026-08-28T09:00:00Z)_
- **[pitfall]** Merge order (measured with `git merge-tree --write-tree`, no worktree touched): this branch's base is the benchmark branch tip merged with main, so PR #6 lands first. `feat/x/lsp-server` and `feat/x/lsp-mirrors-and-release` conflict textually in `src/parser.zig` (`c_decl_modifiers` - keep this wave's added rows, take their alignment; both sides are `zig fmt` clean now) and at the end of `src/index.zig` (both append tests - take both sides), AND produce ONE break git does not report: their `assemble` entry point at `src/index.zig:385` calls `buildJavaBaseTable`, which this wave renamed to `buildTypeBaseTable` (`src/index.zig:1139`, called from `:244`). Whoever merges must retarget it and check `assemble`'s pass list against `buildOpenDir`'s, or the LSP server serves pre-wave resolution. `feat/x/tree-sitter-backend` merges with ZERO textual conflicts and still fails to compile: it adds a trailing `backend` argument to `build`/`buildOpenDir`, and this wave adds 16 new `build(testing.allocator, io, root, false)` call sites (84 -> 100 since the base). Its `model.Symbol.declared_type` also overlaps this wave's `Symbol.bindings` field-type mechanism - `resolveQualified` has to be told which wins. Land this wave BEFORE both: going second costs more than the two textual conflicts above.  _(2026-08-28T09:00:00Z)_
