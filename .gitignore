.nvime/
.advantage/
.zig-cache/
zig-out/
.navgraph/
coverage-out/
.claude/
# generated visualization output at the repo root (navgraph graph > graph.html);
# src/viz.html is the template and is tracked (this pattern is root-anchored).
/*.html
zig-pkg/
